syntax = "proto3";

package oan.oanagent;

import "amino/amino.proto";
import "gogoproto/gogo.proto";
import "oan/oanagent/params.proto";
import "oan/oanagent/agent.proto";

option go_package = "oan/x/oanagent/types";

// GenesisState defines the oanagent module's genesis state.
message GenesisState {
  
  // params defines all the parameters of the module.
           Params params    = 1 [(gogoproto.nullable) = false, (amino.dont_omitempty) = true];
  repeated Agent  agentList = 2 [(gogoproto.nullable) = false] ;
}

